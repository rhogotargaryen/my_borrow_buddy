User Info:
<ul>
<% show_obj_info(@user).each do |x| %>
 <li><%= x %></li>
<% end %>
</ul>

Items:
<ul>
<% @user.items.each do |x| %>
<li><%= link_to "#{x.name}", item_path(x)%></li>
<% end %>
</ul>

</br>

<%= link_to "delete user", @user, method: :delete, data: { confirm: "click OK to confirm deletion of user" } %>