
</br>
 <% if current_user %>
   Hello, <%= current_user.name %> </br>
 <% else %>
     please <%= link_to 'log in', signin_path %>
 <% end %>
</br>   
  

INDEXING ALL <%= "#{@user.name}'s" if @user %> ITEMS: </br>
<% @items.each do |x| %>
  </br>
  <%= x.name %>
  <%= link_to "show item", item_path(x) %>
<% end %> 
</br>



<% if current_user %>
  
</br> Borrowable items</br>

  <%= render'borrowable_items', al_items: Item.all %>
<% end %>
</br>

 
<% if @user && @user == current_user %> 
  
  </br>
    Items allowed to borrow
  </br>
  <%= render 'allow_borrow' %>
  </br> Items borrowed to be returned
  </br>

  <%= render 'returnable_items' %>
  </br>
    Accept a return of your borrowed out item
  </br>
  <%= render 'allow_return' %>
  </br>
  
These are your items out on loan:
</br>
  <% @o_items.borrowed.map do |x| %>
    </br><%= link_to "view #{x.name}", item_path(x) %>: borrowed by: <%= x.borrower.name %>
  <% end %>
</br>
 

<% end %>    