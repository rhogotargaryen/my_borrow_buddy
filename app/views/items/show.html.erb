SHOWING <%= @item.name%> </br>
<% show_item_info(@item).each do |x| %>
  <%= x %></br>
<% end %>


</br>
TEST
</br>

status: <%= @item.requested %>

</br>
END TEST
</br>
<% if @item.owner_id == current_user.id %>

  </br><%= button_to "delete #{@item.name}", @item, method: :delete, data: { confirm: "click OK to confirm deletion of user" } %>
 <% end %>