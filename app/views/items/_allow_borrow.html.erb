  <% @o_items.allow_borrow.map do |x| %>
    <% if x.owner_id == current_user.id %>
      </br><%= button_to "allow borrow  #{x.name}", new_item_transaction_path(x), params: { category: "allow_borrow", recipient_id: x.id_recipient } %>
    <% end %>
  <% end %>