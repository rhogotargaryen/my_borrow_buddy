  
  <% @items.borrowed.map do |x| %>
    <% if x.owner_id != current_user.id %>
     </br><%= button_to "request return #{x.name}", new_item_transaction_path(x), params: { category: "submit_return", recipient_id: x.id_recipient } %>
    <% end %>
  <% end %>